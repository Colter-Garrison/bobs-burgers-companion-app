import React from 'react'
import { ThemeToggleButton } from 'bobs-burgers-components'

// Sun icon in light mode (moon in dark); toggles and persists the theme.
export const Default = () => <ThemeToggleButton />
