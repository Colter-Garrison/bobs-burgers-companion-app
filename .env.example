# Base URL of the server/ backend (auth + favorites). .env holds the
# committed localhost default for running the server/ dev server
# locally; if you need a different value (e.g. a LAN IP for testing on
# a physical device), put it in .env.local instead (gitignored).
EXPO_PUBLIC_API_URL=http://localhost:3000
